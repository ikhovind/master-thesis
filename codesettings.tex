% codesettings.tex
\usepackage{minted}
\usepackage{xcolor}

% 1. Choose a global style (e.g., 'tango', 'friendly', 'pastie', or 'manni')
\usemintedstyle{tango} 

% 2. Define global minted settings
\setminted{
    fontsize=\footnotesize,
    breaklines=true,
    breakanywhere=false,
    linenos=true,
    numbersep=5pt,
    frame=lines,
    framesep=2mm,
    python3=true,
    tabsize=2
}
% 3. IF minted misses a word, you can force it to be a keyword like this:
% This is the minted equivalent of "morekeywords"
\AtBeginEnvironment{minted}{\catcode`\_=12 } % Fixes underscores in some environments
