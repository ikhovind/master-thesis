\chapter{Design} \label{cTapsApiDesign}


\section{Choice of Scope}
One of the key reseach questions motivating the development of CTaps is exactly
how well the TAPS API as it exists currently can be presented programmatically.
CTaps is therefore to present as much of the TAPS API as possible, despite
implementing several features as no-ops.

As it stands now, CTaps implements a large subset of the TAPS API and presents
several of the most beneficial features of the TAPS API such as candidate gathering
and candidate racing, as well as support for TCP, UDP and QUIC. In addition
it presents all of the core objects of the TAPS API, such as Connections, Listeners and
Connection Groups. It allows the application to set all Transport Properties listed
in RFC 9622, although not all properties are supported internally and setting them has no effect.
When performing
racing the most desirable protocols are attempted first and staggered racing
is performed for less desirable protocols, as described in RFC 9623
\cite{brunstromImplementingInterfacesTransport2025}.
\todo{Check in on this later, describe how large of a subset}

The parts of the TAPS API which were prioritized for CTaps are common features related
to establishing client and server connections, transferring data and managing
the lifetime of Connections. The highest prioritized internal TAPS features
were the most unique parts of the TAPS API, which only a software library
presenting a generic interface to the transport layer could present. This
is naturally candidate gathering and candidate racing, which allows a single
client to be able to connect to multiple different endpoints using different
protocols.

Certain parts of the TAPS API are not present in CTaps, the
largest of which is the peer to peer connection functionality.
Implementing this was deemed to be out of scope, while presenting
the API as a no-op was deemed to be of little value, since CTaps would not be able to predict
how the internal rendezvouz implementation would affect the external API.

Other functionality which is not present in the CTaps API is, among others,
being able to add endpoints to an active Connection.
This is omitted because an actual implementation was deemed
out of scope, and presenting it as a no-op was deemed to be of little value.

Overall, CTaps serves as a reference
to show the architectural and design decisions which are necessary when
implementing the TAPS API, as well as to show which features of QUIC can
be exposed through TAPS.

\section{Choice of dependencies}
\subsection{libuv}
Since TAPS is an asynchronous API at its core, CTaps needed to
reflect this. This could have been achieved in several ways,
primarily through internal threading or an event loop. For
CTaps, the event loop was found to have several advantages
primarily avoiding race conditions and not having to guard shared
state, since a single threaded event loop ensures that state
is never accessed concurrently.

When selecting a specific event loop library libuv \cite{Libuv2025} presented
several advantages such as built-in protocol handles such as TCP
and UDP, in addition to being a mature library which is already
used in production systems such as Node.js \cite{nodejsEventLoop}.

Libuv is also the event loop engine behind
NEAT, which influenced the decision of CTaps \cite{neatCode}.

Internally in CTaps, libuv provides the asynchronous interface
which CTaps uses to interact with the network. It also provides
a timer, which is used to query Picoquic at given intervals
for new packets to send.

\subsection{Picoquic}
Since QUIC is not currently supported by libuv or by the Linux kernel
an additional external library was needed in order to add QUIC support
to CTaps. Here, the largest priority was not performance but instead
exploring how QUIC features map onto TAPS.

Picoquic \cite{christianhuitemaPicoquic} was therefore chosen because
of its simplicity and was
prioritized over larger and more performance-oriented libraries such as
MsQuic \cite{MsQuic} or \cite{XQUIC} from Alibaba.

A large advantage of Picoquic is that it features two modes of operation.
It can either be used independently with its own event loop, or be
integrated with an existing one by querying at given timer intervals.
The first mode allowed the benchmarking to be performed with a Picoquic
client completely independent of CTaps, while the second mode allowed Picoquic
to integrate with libuv, without having to manage two event loops.

Internally in CTaps Picoquic serves as a quic engine, being responsible
for generating the QUIC packets which CTaps then sends across the network
through libuv.

\subsection{GLib} \label{sec:glib-design-reason}
CTaps uses GLib \cite{GLibGitLab} for implementations of
hash maps, linked lists and arrays.

GLib was added as a dependency as C has a small standard library
which does not feature any of the data structures mentioned.
If CTaps were to implement all the data structures internally
it would create a large surface area liable for bugs requiring
extensive testing, increasing the scope of CTaps.

GLib itself was chosen because it is a large and mature library
which is featured in several other large well known software libraries,
such as GTK \cite{gtk_website}.

\subsection{log.c}
In order to create structured logs which can
easily be filtered by level
CTaps uses log.c \cite{logc}.
log.c was specifically chosen because of its simplicity
and pure C implementation, making it easy to integrate
with CTaps.

\todo{What is the architecture and why did we choose it.}
\section{Architecture of CTaps}
As mentioned, CTaps aims to conform to the asynchronous TAPS API described
in RFC 9622 \cite{rfc9622}. Since CTaps is asynchronous it has
to rely on the result of any asynchronous operation being passed via
callback, which affects the look of the external API and
the internal architecture.

In addition to this, CTaps is designed to be extensible when it comes to
adding new protocols, and therefore relies an a shared protocol API internally.
In order to keep the implementations as small as possible, common logic such as
state tracking
is extracted to the Socket Manager, which acts as an intermediary and
maintains the lifetime of the sockets.

An overview of the architecture of CTaps can be seen in \cref{fig:ctaps-arch}.
Here, all the core aspects of CTaps can be seen, with the Socket Manager
acting as an intermediary between the CTaps API and the
protocol interface, which provides a common API to the different
protocol implementations. The unique consideration of QUIC can
also be seen, requiring Picoquic as an external library due to lack
of support in libuv.

This section will now expand on these key concepts which have shaped the
architecture of CTaps and the reasoning behind each of them.

\begin{figure}[hptb]
\centering
\begin{tikzpicture}[
    node distance=0.5cm,
    layer/.style={
        draw, 
        line width=0.8pt, 
        minimum width=8cm, 
        minimum height=1.2cm, 
        align=center, 
        fill=gray!5,
        font=\sffamily\bfseries
    },
    sublayer/.style={
        draw, 
        line width=0.6pt, 
        minimum width=3.8cm, 
        minimum height=1cm, 
        fill=white,
        font=\sffamily\small
    }
]

% 1. User Application Layer
\node [layer, fill=blue!10] (app) {Application};

% 2. CTaps API Layer
\node [layer, below=of app] (api) {CTaps API\\ \mdseries\small (TAPS interface, Callback Registration)};

% 3. Socket Manager / Abstraction
\node [layer, below=of api] (manager) {Socket Manager\\ \mdseries\small (Owns network socket)};

% 3. Socket Manager / Abstraction
\node [layer, below=of manager] (interface) {Protocol Interface\\ \mdseries\small (Common interface to network protocols)};

% % 4. Implementation Engines (Split)
\node [sublayer, below=0.5cm of interface.south west, anchor=north west] (uv) {libuv \mdseries\small (TCP/UDP)};
\node [sublayer, below=0.5cm of interface.south east, anchor=north east] (pico) {Picoquic \mdseries (QUIC)};

\node [sublayer, below=0.5cm of pico.south east, anchor=north east] (libuvraw) {libuv \mdseries (raw UDP socket)};

% % 5. OS Layer
\node [layer, below=1cm of libuvraw.south west, fill=gray!20] (os) {Network Stack};

% Connectors
\draw[->, thick] (app) -- (api);
\draw[->, thick] (api) -- (manager);
\draw[->, thick] (manager) -- (interface);
\draw[->, thick] (interface.south) -- ++(0,-0.15) -| (uv);
\draw[->, thick] (interface.south) -- ++(0,-0.15) -| (pico);
\draw[->, thick] (uv) -- (uv |- os.north);
\draw[->, thick] (pico) -- (pico |- libuvraw.north);
\draw[->, thick] (libuvraw) -- (libuvraw |- os.north);
\end{tikzpicture}
\caption[CTaps Architecture Overview]
{
An overview of the architecture of CTaps, featuring the Socket Manager as common as a
mediator between the external functions and the API, with the common protocol interface 
and how it interacts with Picoquic and libuv.
}
\label{fig:ctaps-arch}
\end{figure}

\subsection{Mapping QUIC to TAPS} \label{sec:quic-arch-mapping}
While the TAPS RFCs were designed with QUIC in mind, it was written before the
QUIC design was finalized in RFC 9000 \cite{rfc9000}. This means that RFC 9623 does not
feature any specific transport protocol consideratoins for mapping QUIC into
a TAPS system \cite{brunstromImplementingInterfacesTransport2025}.
There exists a draft for mapping Transport Services to QUIC, but this has
not made it into the official RFC \cite{transportServicesMappingForQuic}.
CTaps has
therefore had to make independent design decisions on exactly how to
map QUIC into TAPS.

The most important design decision made in CTaps in regards
to QUIC was to map a QUIC Connection
to a Connection Group instead of to a Connection. Since
Connection Groups are populated by Connection cloning, and RFC
9622 says \say{if the underlying protocol supports multi-streaming, it is natural to use this functionality to
implement \mono{Clone}} \cite[Sec. 7.4]{rfc9622}, the Connection Group
becomes the natural model for the QUIC Connection.

CTaps could
map QUIC connections to a TAPS Connection, but this would make
multistreaming unavailable through the TAPS API, since TAPS
features no equivalent concept of a stream within a Connection.
Multistreaming is a core benefit of QUIC and allows for more
efficient bandwidth usage, as showcased in \cref{sec:smallfiletransfer}.

The consequence of this mapping is that the behavior 
when closing a single Connection depends on the underlying
protocol. For TCP, closing a CTaps Connection will initiate
teardown of the entire connection, and no more data will be
delivered after the \mono{closed()} callback has bene invoked.

Since QUIC features no way of gracefully closing
a single stream other than both peers sending a FIN flag,
CTaps potentially has to let a Connection remain open
and cannot invoke the \mono{closed()} callback until
the peer has closed the sending side. This means
that an application has to monitor a Connection for
incoming data, until the peer finishes sending.

This differing behavior can also be mitigated by only interacting
with Connection Groups when wanting to shut down a logical connection,
which is compatible with how TCP support is implemented.
This is discussed in detail in \cref{sec:quicUnderTaps}.

\subsection{Socket Manager} \label{sec:socket-manager-design}
The Socket Manager is an internal structure in CTaps responsible
for acting as an asynchronous intermediary between the API and the protocol implementations,
as well as managing the lifetimes of the underlying sockets.

The Socket Manager has to be compatible with both multiple unrelated
Connection Groups using a single Socket, such as when listening
on an UDP socket, as well as Connections in the same Connection
Group using different sockets, such as when cloning TCP connections.
Therefore, the Socket Manager is
referenced on the Connection level and a single shared Socket Manager is created
for each used Socket.
Since UDP Connections in CTaps can share a single socket the 
Socket Manager is also responsible for demultiplexing incoming messages
as UDP is not connection-oriented.

Since several functions within the various protocol implementations
are asynchronous, communication between the protocol implementation
and the socket manager has to be asynchronous as well. The Socket
Manager features a structure with internal callbacks which are invoked by the protocol
implementation at appropriate times, for example to notify
the Socket Manager that a Connection has been closed after teardown
has completed.

This pattern of the protocl implementation exclusively speaking
to the Socket Manager only applies to memory-critical paths, in order
to mitigate
potential memory leaks.

An example of such a memory-critical internal flow in CTaps can be seen in
\cref{fig:async-close-flow}, showing the asynchronous flow of
graceful connection teardown for a Connection using a shared
Socket Manager. Here, a single Connection in a Connection Group
is closed, and the Socket Manager invokes the application callback
asynchronously.

The figure also shows that crucially the Socket Manager keeps
the socket open, so that it can be used by the remaining Connections.

\definecolor{ConnectionGroupTextColor}{RGB}{139, 194, 110}
\begin{figure}[ph]
\centering
\begin{tikzpicture}[font=\sffamily,
    block/.style={rectangle, draw, text centered, rounded corners, fill=white, font=\bfseries},
    client/.style={rectangle, draw, text centered, fill=white, font=\small},
    arrow/.style={-Stealth, thick},
    asyncArrow/.style={arrow, dashed, red!70!black},
    label/.style={font=\scriptsize\itshape}
    ]

% Place connections first
\node[client] (Conn1) at (0,0) {Connection 1};
\node[client, below=0.3cm of Conn1] (Conn2) {Connection 2};
\node[client, below=1.2cm of Conn2] (ConnN) {Connection N};

% SM centred vertically on the connections group
\node[block, align=center] (SM) at
    ($(Conn1.north east)!0.5!(ConnN.south east) + (4,0)$)
    {Socket Manager};

\node[block, right=of SM] (Prot) {Protocol Impl};

\draw[arrow] (Conn1.north east) -- node[above, sloped, label, yshift=3pt] {close()} (SM.north west);
\draw[arrow] (Conn2.east) -- (SM.west);
\draw[arrow] (ConnN.east) -- (SM.south west);

\node[font=\Huge, text=gray] at ($(Conn2.south)!0.4!(ConnN.north)$) {$\vdots$};

\begin{scope}[on background layer]
    \draw[rounded corners, fill=blue!5!white, draw=blue!20]
        ($(Conn1.north west)+(-0.3, 0.4)$) rectangle ($(ConnN.south east)+(0.3,-0.3)$);
    \node[font=\normalsize\bfseries, above=0.4cm of Conn1, blue!70!black, align=center]
        {Connections};
    \node[fit=(Conn1)(Conn2), rounded corners, fill=green!5!white,
          draw=green!20, inner sep=0.13cm] (GreenBox1) {};
    \node[fit=(ConnN), rounded corners, fill=green!5!white,
          draw=green!20, inner sep=0.13cm] (GreenBoxN) {};
\end{scope}
\node[font=\tiny\bfseries, text=ConnectionGroupTextColor, above=-1.2mm of GreenBox1] {Connection Group 1};
\node[font=\tiny\bfseries, text=ConnectionGroupTextColor, above=-1.2mm of GreenBoxN] {Connection Group N};

% Request Flow
\draw[arrow]
    ([yshift=-0.5mm]SM.north east)
    -- node[above, sloped, label, align=center] {Close connection}
    ([yshift=-0.5mm]Prot.north west);
\draw[asyncArrow] 
    ([yshift=0.5mm]Prot.south west)
    -- node[below, label, text=red!70!black, align=center] {Socket Manager cb}
    ([yshift=0.5mm]SM.south east);
\draw[asyncArrow]
    ([yshift=4pt]SM.west)
    -- node[below, sloped, label, text=red!70!black, align=center] {User cb}
    ([yshift=3pt]Conn1.east);
\end{tikzpicture}
\caption[Flow of internal callbacks in CTaps.]{Example model of the asynchronous communication
between the CTaps Socket Manager and the protocol implementation when an application invokes \mono{close()}}
\label{fig:async-close-flow}
\end{figure}

\subsection{Protocol Interface}
In order to make the CTaps design as modular as possible,
CTaps uses an internal interface to interact with the underlying protocol.
This interface abstracts away the implementation details of the individual
protocols and allows the Socket Manager to not have to implement protocol
specific logic, making it far easier to extend support to more protocols in
the future.

An excerpt of the protocol interface struct can be seen in
\cref{lst:protocol-impl-struct}. Here the \mono{supports_alpn} flag
and the Selection Properties can be seen. These are used
to encode the capabilities of the protocol when defining an
instance of the interface. The encoded capabilities 
allows CTaps to filter and sort protocols according to the
Transport Services desired by the application, while
the implemented function pointers present a generic
interface to protocol-specific behavior.

\begin{listing}[hptb]
\begin{minted}{C}
typedef struct ct_protocol_impl_s {
    const char* name;                               ///< Protocol name (e.g., "TCP", "UDP", "QUIC")
    ct_protocol_enum_t protocol_enum;               ///< Protocol enumeration value
    bool supports_alpn;                             ///< True if protocol supports ALPN negotiation
    ct_selection_properties_t selection_properties; ///< Properties supported by this protocol
    /** @brief Initialize a new connection using this protocol. */
    int (*init)(ct_connection_t* connection);
    /** @brief Send a message over the protocol. */
    int (*send)(ct_connection_t*, ct_message_t*, ct_message_context_t*);
} ct_protocol_impl_t;
\end{minted}
\caption{Excerpt from the protocol Implementation interface}
\label{lst:protocol-impl-struct}
\end{listing}


% TODO kom hit -> What and why

\section{API Design}
This section will describe design decision related to the external API
of CTaps.
A full overview of the API, which objects are available and how they
map to TAPS can be seen in the in the attached doxygen output in the
appendix. \todo{Add to appendix}
\todo{What do we present to the user and why}
\subsection{Object Orientation}
In oder to present a consistent API to downstream application,
CTaps follows a design philosophy to match the design
described in RFC 9622 as closely as possible \cite{rfc9622}
The abstract interface described in this RFC is object
oriented and to match this, CTaps emulates object
orientation as described in \cref{sec:object-orientation-in-c}.

An example of this
is the struct representing Remote Endpoints which can be seen in \cref{lst:remote-endpoint}.
This object contains all the context related to remote endpoints, which then is used
in Connections and Listeners. The object has dedicated functions to operate
on it, and therefore encapsulates all responsibilities related to Remote Endpoints.

The CTaps API consists of a single header file, and all CTaps objects are contained
as forward declarations within this file
The application then has to interact with the objects through a list of functions
which are use to allocate a struct on the heap and to modify the object using
its pointer.
This pattern can be seen in \cref{lst:using-remote-endpoint}.

Not exposing the definitions of the sturcts 
to the application allows CTaps defines which fields are supposed to be external,
by controlling getters and setters in the external API.
This also helps future-proof the API, as renaming and adding or
removing internal members of a struct does not affect downstream applications.

Because all objects in CTaps are allocated on the heap in CTaps using \mono{\*\_new()}
functions it is important that the client frees the allocated object at the appropriate time.
In order to make this as intuitive and consistent as possible CTaps has taken the deliberate
design decision to never take ownership over any of these objects when they are passed as parameters
to a CTaps function. CTaps is then responsible for freeing its internal copies when appropriate.

This also means that if the application
knows that the object it has allocated will not be used again, then it can
be freed after the last
function taking it as a parameters returns.

All CTaps objects have
dedicated destructors and it would therefore be be possible for each object
to contain an internal reference count ensuring the lifetime of the passed
objects. Deep copying parameters was however chosen over reference counting
since the objects in the external CTaps API are relatively small, so the simplicity
of deep copying was deemed more important than the efficiency of reference
counting.

QUIC also affected the architecture and ownership model of CTaps, as it
was the only supported protocol which where the per-Connection Group
state differs from the per-Connection state, it forced CTaps to introduce
a clearer separation of conecerns with a separate void pointers to be
used for internal protocol state in the Connection, Connection Group and
Socket Manager structures. It also caused CTaps to make every Connection a
part of a Connection Group, even if it has not been used for cloning. This
was done in order to both present a unified interface to applications using
CTaps, as well as avoiding internal checks for whether a Connection was
part of a Connection Group or not.

\begin{listing}[phtb]
\begin{minted}{C}
typedef struct ct_remote_endpoint_s {
    ///< Port number set by user
    uint16_t port;
    ///< Service name (e.g., "https") or NULL
    char* service;
    ///< Hostname for DNS resolution or NULL
    char* hostname;
    ///< Resolved socket address
    struct sockaddr_storage resolved_address;
} ct_remote_endpoint_t;

// Example functions
ct_remote_endpoint_t* ct_remote_endpoint_new(void);
int ct_remote_endpoint_with_hostname(
    ct_remote_endpoint_t* remote_endpoint,
    const char* hostname);
\end{minted}
\caption{Remote endpoint as represented in CTaps}
\label{lst:remote-endpoint}
\end{listing}

\begin{listing}[phtb]
\begin{minted}{C}
    // Step one allocate object on the heap
    ct_remote_endpoint_t* remote_endpoint = ct_remote_endpoint_new();
    // Check for allocation error
    if (!remote_endpoint) {
      return -1;
    }

    // Then modify it
    ct_remote_endpoint_with_ipv4(remote_endpoint, inet_addr("127.0.0.1"));
    ct_remote_endpoint_with_port(remote_endpoint, 1234); // example port

    // ... Then use it in a Preconnection for example...


    // And finally free it when done
    ct_remote_endpoint_free(remote_endpoint);
\end{minted} 
\caption{Remote endpoint lifetime example}
\label{lst:using-remote-endpoint}
\end{listing}

\subsection{External Error Handling}
The error handling presented to the application largely follows
the same structure as described in \cref{sec:internal-error-handling}
with two major differences. Firstly, CTaps verifies the input parameters
of functions presented in the external API, meaning that they return an
error value, usually -EINVAL, if a NULL pointer or othervise invalid value
is passed to a mandatory parameter. The second change is that in the
external API CTaps features dedicated error callbacks.

This is done to present a more intuitive API to the application and
to fail early, making it easier to detect for example when a mandatory parameter
is omitted. Having dedicated error callbacks also allows the application to not
have to write error handling code in the success callbacks.

\subsection{Library Lifetime}
Internally, CTaps uses a global event loop, which has to be initialized
before operation can be performed. The application has to do this through the
setup function \mono{ct_initialize()}. This allocates memory for the event loop,
initiates it and sets a default log level.

After initializing CTaps, the event loop can be
started using \mono{ctaps_start_event_loop()}. This event loop will run until
there are no more open handles performing work. Any set up of Connections
which should be used during the lifetime of a given event loop has to
be performed before starting the event loop. The event loop takes over
execution of the main thread, and will block until execution of the underlying
handles completes, as described above.

This means that if the application invokes \mono{ct_preconnection_initiate()},
then CTaps will attempt to connect to the specified endpoint only when the event
loop is started.

Any behavior that has to happen after an event loop is created has to be
specified in the callbacks provided by CTaps. For example, if a Connection
has to immediately send a message after successfully connecting to an endpoint,
then this behaviour has to be defined in the \mono{ready()} callback.

Because CTaps allocates memory for the global event loop this also has to
be freed in order to prevent memory leaks. This is done through the
CTaps teardown function \mono{ct_close()}. This closes the event loop
and frees it synchronously.

\subsection{Single-threaded async model} \label{asynchrony}
Because CTaps uses libuv to interact with the network, the external API
of CTaps also has to be callback-based. The application can register callbacks
with CTaps at key locations in the API.

When registering callbacks in CTaps this is done
as close
to the relevant event as possible. Connection callbacks 
are registered when the Preconnection is initiated, Listener
callbacks are registered when invoking \mono{ct_preconnection_listen()} etc.

In order to allow the user to pass data into the callback, all callback structures
contain a void pointer which can be set by the user. This void pointer
is then passed as a parameter when CTaps invokes the callback. This allows the user
implementation of the callback to dereference the void pointer
and pass any data to the callback, to make it accessible in
the body of the callback itself.

CTaps is not designed for multi-threading in mind and does not make use
of the threading API available through libuv \cite{libuv_threading}. This
means that there is no internal synchronization and it expects applications
to only interact with CTaps through a single thread, which eventually will
start executing the event loop.

\subsection{Listener}
Listeners are
created asynchronously because they share the Candidate Gathering
process as normal Connection initiation.

\subsection{Connection Termination}
Since both aborting and closing Connections may involve over-the-wire teardown,
all Connection termination is treated asynchronously by CTaps, even when it only involves
closing a socket synchronously.

Since a QUIC Connection maps onto a TAPS Connection Group, the underlying
behavior when closing a Connection which uses QUIC as its underlying protocol
may be surprising as discussed in \cref{sec:quic-arch-mapping}. 
